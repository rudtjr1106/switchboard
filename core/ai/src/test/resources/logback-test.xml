<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{0} - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- llama/ggml 메시지는 trace 라 안 보인다. 통합 테스트의 로드 시간·tok/s 는 info 로 남긴다 -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
